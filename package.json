{
  "name": "juku-web",
  "private": true,
  "version": "0.1.5",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "pwa:sync": "node ./scripts/sync-pwa-version.mjs",
    "build": "node ./scripts/sync-pwa-version.mjs && vite build && node ./scripts/finalize-pwa-build.mjs",
    "preview": "vite preview",
    "deploy:package": "node ./scripts/package-deploy.mjs"
  },
  "dependencies": {
    "three": "0.183.2"
  },
  "devDependencies": {
    "vite": "^7.3.1"
  }
}
