{
  "name": "juku-web",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "pwa:sync": "node ./scripts/sync-pwa-version.mjs",
    "build": "node ./scripts/sync-pwa-version.mjs && vite build",
    "preview": "vite preview",
    "deploy:package": "node ./scripts/package-deploy.mjs"
  },
  "dependencies": {
    "three": "^0.180.0"
  },
  "devDependencies": {
    "vite": "^7.1.5"
  }
}

